#!/usr/bin/env python3
import os
import json
import urllib.request
import urllib.parse
from dotenv import load_dotenv

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
load_dotenv(os.path.join(BASE_DIR, '.env'))

token = os.getenv("BOT_TOKEN")
channel = os.getenv("CHANNEL_ID") or os.getenv("CHANNEL_USERNAME", "@toplivo_ykt")

text = """⛽ <b>Оперативная сводка по топливу в г. Якутске</b>
📅 15 сентября 2026 года

Ситуация на АЗС города стабильная — острый дефицит бензина спал, большинство станций отпускают топливо без очередей. Основное внимание уделяется правилам отпуска и ситуации на АГЗС.

📊 <b>Обстановка по маркам:</b>
• 🟢 <b>АИ-92</b> — свободно в большинстве сетей (СНГС, СибОйл, Туймаада-Нефть, ОПТИ). На кассах действуют стандартные лимиты 20–30 л в бак.
• 🟢/🟠 <b>АИ-95</b> — отпускается стабильно. Обратите внимание: в «Туймаада-Нефть» 95-й отпускается строго через мобильное приложение по QR-коду на колонке (до 20 л/сутки).
• 🔴 <b>АИ-98</b> — в городе отсутствует на всех сетях. Мониторинг ведется непрерывно.
• 🟢 <b>Дизтопливо (ДТ)</b> — в наличии без ограничений и очередей.
• 🟡 <b>Газ (СУГ / Метан)</b> — действуют дневные АГЗС (Можайского 31, пр. М. Николаева 16а, ПАГЗС Жатай) и ночная (Вилюйский тр. 4 км). На газовых станциях возможны очереди в часы пик.

📱 <b>Правила ключевых сетей:</b>
• <b>Туймаада-Нефть:</b> АИ-92 — за нал/карту через кассу (лимит 20 л). АИ-95 — строго по QR в приложении. На Покровском тр. 5 км и пр. Николаева 31а отпуска 95-го по приложению нет.
• <b>СибОйл:</b> на АЗС Покровский тр. 4 км, Вилюйский тр. 6/1 и ул. Окружная отпуск АИ-92/95 открыт за наличные и по банковским картам (лимит 20 л). На остальных точках — преимущественно по картам юрлиц.
• <b>Саханефтегазсбыт (СНГС):</b> на кассах лимит 20 л бензина, 50 л ДТ. АЗС №51 (ГРЭС) — 30 л / 200 л. Автоматические АЗС — только по картам.

🎈 <b>Бытовой газ (баллоны 5–50 л):</b>
Обмен баллонов СУГ производится на станциях СТНГ (поверенные баллоны ЯГПЗ), а также на пунктах Green Line и Энерготрейд. Прямой налив в непроверенную тару запрещен ТБ.

🗺 <b>Интерактивная карта наличия и цен в реальном времени:</b>
👉 https://fuel-map.tiinservice.online/"""

data = urllib.parse.urlencode({
    "chat_id": channel,
    "text": text,
    "parse_mode": "HTML",
    "disable_web_page_preview": "false"
}).encode("utf-8")

url = f"https://api.telegram.org/bot{token}/sendMessage"
req = urllib.request.Request(url, data=data, headers={"Content-Type": "application/x-www-form-urlencoded; charset=utf-8"})
with urllib.request.urlopen(req) as resp:
    res = json.loads(resp.read().decode())
    print("Post 1 published successfully:", res.get("ok"))
